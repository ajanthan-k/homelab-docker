
services:
  caddy:
    image: docker.io/technoguyfication/caddy-cloudflare:latest@sha256:aede1bef0c900b34eb8a673b468f633165b62b4d7118673e39016ffce2a0645d
    container_name: caddy
    restart: unless-stopped
    ports:
      - 80:80
      - 443:443
    environment:
      - MY_DOMAIN
      - CLOUDFLARE_API_TOKEN
      - ACME_AGREE=true
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./caddy_config:/config
      - ./caddy_data:/data

networks:
  default:
    name: caddy_proxy
    external: true
